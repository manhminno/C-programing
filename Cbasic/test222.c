#include <stdio.h>
#include <string.h>

int main(){
    int a, b;
    scanf("%d", &a);
    char c;
    c = getchar();
    c = getchar();
    c = getchar();
    scanf("%d", &b);
    printf("%d %d", a, b);
    return 0;
}